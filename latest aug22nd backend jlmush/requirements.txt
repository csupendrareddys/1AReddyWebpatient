# Healthcare Backend - Production Requirements
# Only packages actually used in the application

# Flask Core
Flask==3.1.2
Werkzeug==3.1.3
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.3.0

# Database
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.43
Flask-Migrate==4.1.0
alembic==1.17.0
psycopg2-binary==2.9.10

# Authentication & Security
Flask-JWT-Extended==4.7.1
PyJWT==2.10.1
Flask-Login==0.6.3
cryptography==46.0.2

# Redis (sessions & rate limiting)
redis==6.4.0
Flask-Limiter==3.5.0

# Real-time communication channel (Service Chats).
# Flask-SocketIO speaks the Socket.IO protocol; eventlet is the async worker
# it needs (sync gunicorn workers can't hold WebSocket connections). eventlet
# pulls python-socketio/python-engineio transitively. Redis (above) is reused
# as the Socket.IO message_queue so emits fan out across gunicorn workers.
# NOTE: eventlet.monkey_patch() (see wsgi.py) rewrites the socket layer; verify
# psycopg2 DB calls behave under load. If they stall, switch to gevent +
# gevent-websocket + psycogreen and set the gunicorn worker-class to gevent.
Flask-SocketIO==5.3.6
eventlet==0.35.2

# CORS
flask-cors==6.0.1

# Forms & Validation
Flask-WTF==1.2.2
WTForms==3.2.1
email_validator==2.2.0
marshmallow==4.0.1

# Production Server
gunicorn==21.2.0

# Utilities
python-dotenv==1.2.1
requests==2.32.3  # Cloudflare DNS API client (app/services/cloudflare_dns.py)
dnspython==2.6.1  # TXT-record lookups for custom domain verification (app/services/domain_verification.py)

# AWS S3
boto3==1.38.0

# Payment Gateway
razorpay==1.4.2
setuptools==80.0.0

# Twilio Video (WebRTC video calls)
twilio==9.4.0


# Document processing (HTML to Word conversion)
python-docx==1.1.2
beautifulsoup4==4.13.4

# PDF generation
fpdf2==2.8.3
# Background scheduler for expiry / retention / payout-hold / video-room jobs
APScheduler==3.10.4
# Crash reporting (dormant without SENTRY_DSN env)
sentry-sdk[flask]==2.19.2
