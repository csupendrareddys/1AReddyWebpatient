# mobile-patient-_mvp

Patient mobile app (React Native / Expo SDK 57, expo-router, TypeScript).

Front-end only — every screen reads from the mock data in `src/data/`. There are no network calls and no backend.

## Run it

```bash
npm ci
npx expo start
```
